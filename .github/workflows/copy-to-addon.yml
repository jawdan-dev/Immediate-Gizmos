name: Copy files into addon

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - LICENSE
      - README.md
      - examples/
  pull_request:
    paths:
      - LICENSE
      - README.md
      - examples/

permissions: write-all

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  copy-files:
    name: Copy Files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Copy files
        run: |
          cp LICENSE addons/immediate_gizmos/LICENSE
          cp README.md addons/immediate_gizmos/README.md
          cp -r examples/ addons/immediate_gizmos/
      - name: Commit files
        uses: devops-infra/action-commit-push@v1.0.3
        with:
          github_token: "${{ secrets.GITHUB_TOKEN }}"
          commit_message: "docs: update addon license and readme"